  const IMAGE_WIDTH = 500;
  const IMAGE_HEIGHT = 342;

  function setup() {
      createCanvas(IMAGE_WIDTH, IMAGE_HEIGHT);
  }

  function draw() {
      stroke(198,157,101);
      line(292, 1, 465, 261);
  }
